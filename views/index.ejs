<!DOCTYPE html>

<html lang="end">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Find Image By Colors - 0.1.0</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/css/style.css">
    <link rel="stylesheet" href="/assets/css/colorPicker.css">

</head>

<body>
    <div class="container">
        <div class="row mb-4">
            <div class="col-12"><h1>Find Image By Colors - 0.1.0</h1></div>
        </div>
        <form class="form-horizontal row" method="post" action="/">
            <div class="col-3">
                <div class="control-group">
                    <label>Pick a Color</label>
                    <div class="controls input-prepend">
                        <input type="text" name="color" id="color" class="color input-small"
                            <% if(req) { %>
                               value="<%= req.body.color %>"
                            <% } %>
                        />
                    </div>
                </div>
            </div>
            <div class="col-4">
                <div class="control-group">
                    <label>Inform Distance Between the Colors</label>
                    <div class="controls input-prepend">
                        <input type="text" name="distance" id="distance"
                        <% if(req) { %>
                               value="<%= req.body.distance %>"
                        <% } else { %>
                               value="0"
                                <% } %>
                        />
                    </div>
                </div>
            </div>
            <div class="col-5 mt-3">
                <button class="btn btn-primary">Pesquisar</button>
            </div>
        </form>
        <div class="mt-5 mb-5"><hr></div>
        <div class="row">
            <% photos.forEach(function(photo){ %>
                <div class="col-4">
                    <img src="data:image/jpeg;base64,<%= photo.photo %>" >
                </div>
            <% }); %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/assets/js/jquery.js"></script>
    <script src="/assets/js/jquery.colorPicker.js"></script>
    <script>
        $(function() {
            var $inputs = $('input.color');
            $inputs.colorPicker();
        });
    </script>
</body>